[project]
name = "financial-risk-intelligence-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anupriya Maity", email = "maityanup9892@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.0",
]

[project.scripts]
financial-risk-intelligence-system = "financial_risk_intelligence_system:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "coverage>=7.15.2",
    "pre-commit>=4.6.1",
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]
